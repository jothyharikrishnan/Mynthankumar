#include <iostream>
using namespace std;

int main() {
      char mm;
      cout<<"enter the character\n";
      cin>>mm;
      if(mm=='a'||mm=='e'||mm=='i'||mm=='o'||mm=='u')
      {
      	cout<<"the given character is vowels";
      }
      else
      {
      		cout<<"the given character  is consonants";
      }
	return 0;
}
